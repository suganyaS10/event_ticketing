# Credentials only — do NOT include a database name.
# database.yml supplies ticketing_development / ticketing_test per environment,
# so a database name here would point tests at your development database.
DATABASE_URL="postgres://username:password@localhost:5432"